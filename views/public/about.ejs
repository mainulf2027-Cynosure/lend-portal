<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title }) %>
<body>
<%- include('../partials/public-header', { activePage }) %>
<div id="page-shell">
<div id="breadcrumb"><a href="/">Home</a><span class="bcsep">&gt;</span>About</div>
<div id="main">
  <div class="panel">
    <div class="phead">About — Mainul Fahad</div>
    <div class="pbody" style="padding:12px 14px;">
      <% if (content) { %>
        <div class="rc"><%- content %></div>
      <% } else { %>
        <div class="empty" style="text-align:left;">
          <div style="font-size:13px;font-weight:bold;color:var(--crimson-text);margin-bottom:6px;">Mainul Fahad</div>
          <div style="color:var(--grey-text);">About Me content not yet added. Edit via Admin &gt; About Me.</div>
        </div>
      <% } %>
    </div>
  </div>
</div>
<%- include('../partials/public-footer') %>
</div>
</body>
</html>
